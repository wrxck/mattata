version: "3.9"

services:
  app:
    build:
      context: .
    image: mattata/app:1.0
    volumes:
      - logvolume01:/var/log
    links:
      - redis
  redis:
    image: redis

volumes:
  logvolume01: {}